'use strict';

var expect = chai.expect;

describe('Things and stuff', function () {

	beforeEach(module('angularAssessment'));

	it('system check works', function () {
		expect(true).to.equal(true);
	});

});